/*
 * When the page loads, set the focus to the username input field.  Note that
 * the id of the input element to set focus is 'UserName' in the login.jsp.
 */
window.onload = function() {
    document.getElementById('UserName').focus();
}